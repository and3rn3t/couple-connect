# Configure npm for better CI/CD compatibility
# Rollup platform-specific dependencies should be optional only
optional-dependencies=true

# Improve installation speed
prefer-offline=true
audit=false
fund=false

# Better caching (use prefer-offline instead of deprecated cache-max)
# cache-max has been deprecated in favor of prefer-offline/prefer-online
# prefer-offline=true is already set above
